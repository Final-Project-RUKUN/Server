const request = require('supertest')
const app = require('../app')

describe('TESTING /users', () => {
  let user = {}
  
  it('testing create users with response 201', function(done) {
    
  }) 
})
